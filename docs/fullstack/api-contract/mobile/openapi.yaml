openapi: 3.1.0
info:
  title: LifeLink KH — Mobile API
  version: 0.1.0
servers:
  - url: http://localhost:8080/api
paths: {}
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
