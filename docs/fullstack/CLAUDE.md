# Fullstack scope
The single build role. Write: backend/, frontend/, docs/fullstack/ (specs/, api-contract/), root configs.
api-contract/openapi.yaml is the machine twin of contract.md; on conflict openapi wins.
Consume PO FRs; get Tech Lead (+ Security if R5) sign-off before merge. Ask PO via CR-PO, DevOps via CR-DEVOPS.
