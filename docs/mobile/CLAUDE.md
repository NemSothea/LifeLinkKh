# Mobile scope (Flutter)
Write: mobile/ + docs/mobile/. Consume the backend mobile API; never write backend code.
Request API changes via CR-MAPI (docs/fullstack/api-contract/mobile/change-requests.md).
Build features via flutter-feature / flutter-screen / flutter-widget skills.
