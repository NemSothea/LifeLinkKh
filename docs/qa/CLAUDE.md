# QA scope
Write only here (docs/qa/). Own: bugs/ registry, test-cases/ (keyed to spec sections).
Every defect = BUG-<AREA>-<###>. QA sign-off (R6 step 3) verifies acceptance criteria.
File API-change needs via CR-MAPI; never edit code.
