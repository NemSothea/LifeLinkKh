# Security scope (overlay — held by Tech Lead)
Write only here (docs/security/). Own: threat-models/, reviews/, security-checklist.md.
Run the R6 gate on every R5 change (auth, PII, secrets, external integrations).
Record each review verdict in reviews/ (R6 step 4). Anyone asks via CR-SEC.
