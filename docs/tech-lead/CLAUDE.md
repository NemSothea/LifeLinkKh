# Tech Lead scope
Write only here (docs/tech-lead/). Own: architecture.md, adr/, coding-standards.md, data-model.md.
Also own, outside this folder: `docker-compose.yml`, `.github/workflows/`, the deploy runbook, the release.
Sign off every spec's architecture. Record an ADR for each hard-to-reverse choice.
Also holds the Security overlay for this project (see docs/security/).
