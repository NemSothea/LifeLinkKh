# Placeholder pipeline — owned by Tech Lead. There is no DevOps role and no deploy
# runbook yet; writing one is a blocker for the M7 signed-AAB release (docs/risks.md).
name: ci
on:
  push: { branches: [ main ] }
  pull_request: {}
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: build backend + web, run tests, build Flutter AAB. Real steps land at M2 once backend/ frontend/ mobile/ are scaffolded."
