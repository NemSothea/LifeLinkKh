{
  "name": "lifelink-functions",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test": "vitest run test/unit",
    "test:emulator": "cd .. && firebase emulators:exec --only firestore --project demo-lifelink \"cd functions && vitest run test/emulator\""
  },
  "dependencies": {
    "firebase-admin": "^14.5.0",
    "firebase-functions": "^7.4.0",
    "geofire-common": "^6.0.0"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
